version: "3.8"
services:
  harvey-ui:
    build: .
    restart: always
    networks:
      - traefik
      - harvey-ui
    labels:
      - traefik.enable=true
    env_file:
      - src/.env

networks:
  traefik:
    name: traefik
  harvey-ui:
    name: harvey-ui
