services:
  harvey-ui:
    build: .
    restart: always
    networks:
      - traefik
      - harvey-ui
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik
      - traefik.port=80
    env_file:
      - src/.env
    depends_on:
      - harvey-ui-db
      - harvey-ui-db-replica
  harvey-ui-db:
    image: 'bitnami/mariadb:10.6.7'
    restart: always
    volumes:
      - database-data:/bitnami/mariadb
    environment:
      - MARIADB_REPLICATION_MODE=master
      - MARIADB_SKIP_TEST_DB=yes
    env_file:
      - database.env
    networks:
      - harvey-ui
    labels:
      - traefik.enable=false
  harvey-ui-db-replica:
    image: 'bitnami/mariadb:10.6.7'
    restart: always
    depends_on:
      - harvey-ui-db
    environment:
      - MARIADB_REPLICATION_MODE=slave
      - MARIADB_MASTER_HOST=harvey-ui-db
    env_file:
      - database.env
    networks:
      - harvey-ui
    labels:
      - traefik.enable=false

volumes:
  database-data:
    driver: local

networks:
  traefik:
    name: traefik
  harvey-ui:
    name: harvey-ui
